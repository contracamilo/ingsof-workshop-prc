// Archivo migrado.
// El backend ahora vive en /backend/server.js
// Este archivo se mantiene sólo como aviso para evitar levantar la versión antigua.
console.error('El backend se movió a backend/server.js. Usa: node backend/server.js');
process.exit(1);