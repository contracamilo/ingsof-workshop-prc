{
  "name": "u-la-salle-contact-form-root",
  "private": true,
  "description": "Archivo placeholder. El backend se movió a ./backend",
  "workspaces": ["backend"],
  "scripts": {
    "start": "echo 'Backend se movió a ./backend. Ejecuta: cd backend && npm start' && exit 1"
  }
}